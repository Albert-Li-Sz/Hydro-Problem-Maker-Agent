# A + B contract fixture

`hydro/a-plus-b/` is the exact directory intended for a Hydro import archive. `authoring/` is deliberately outside that directory so importing the fixture cannot publish a solution or trigger an automatic submission.

The fixture is used to test metadata, Chinese statement rendering, grouped samples, a `file://` image, explicit scoring, and the separation between release and authoring artifacts.
